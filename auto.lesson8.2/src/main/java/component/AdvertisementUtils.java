package component;

import org.openqa.selenium.WebDriver;

public class AdvertisementUtils {

    private WebDriver driver;

   // public AdvertisementUtils closeAdvertisementIfPresent(WebDriver driver) {
   //     WebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(15));
    //    WebElement closeButton = wait.until(ExpectedConditions.visibilityOfElementLocated(By.className()));
    //    closeButton.click();
    }


